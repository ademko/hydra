
<% renderErrors(); %>

<% renderNotice(); %>

Current sites:
<p>
<ul>
<% while (dm_site.next()) { %>
  <li>id=<%=dm_site.id%> name=<%=dm_site.name%> desc=<%=dm_site.desc%>
    <%=linkTo("Edit Site", &Sites::edit, dm_site.id)%>
    <%=linkTo("Delete Site", &Sites::destroy, dm_site.id)%>
  <% Host subhost = dm_site.host();
     while (subhost.next()) { %>
       host=<%= subhost.hostname %>
  <% } %>
  </li>
<% } %>
</ul>

<%= linkTo("New Site", &Sites::create); %>
<p>

