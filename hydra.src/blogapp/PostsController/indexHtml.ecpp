
<ul>
<% while (dm_post.next()) { %>
  <li><%=linkTo(dm_post.id, pathTo(&PostsController::show, dm_post))%> <p>
  <% output() << MarkDown::process(dm_post.asByteArray(), MarkDown::Format_Post); %>
  </li>
<% } %>
</ul>

